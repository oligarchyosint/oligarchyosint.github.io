---
title: Welcome
---

This site is built with [Quartz](https://quartz.jzhao.xyz/). Add Markdown files under `content/` and push; [GitHub Actions](https://github.com/features/actions) runs `npm ci` and `npx quartz build`, then publishes the `public` output to GitHub Pages.
